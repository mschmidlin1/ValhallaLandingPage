apiVersion: v1
kind: Service
metadata:
  name: valhallalandingpage
  namespace: valhallalandingpage-dev
spec:
  type: ClusterIP
  selector:
    app: valhallalandingpage-dev
  ports:
    - port: 80
      targetPort: 80
